<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
android:paddingRight="@dimen/activity_horizontal_margin"
android:paddingTop="@dimen/activity_vertical_margin"
android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

<Button
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="User Profile"
    android:id="@+id/profileButton"
    android:layout_alignParentTop="true"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="78dp" />

<Button
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Calendar"
    android:id="@+id/datesButton"
    android:layout_marginTop="50dp"
    android:layout_below="@+id/profileButton"
    android:layout_alignLeft="@+id/profileButton"
    android:layout_alignStart="@+id/profileButton"
    android:layout_alignRight="@+id/profileButton"
    android:layout_alignEnd="@+id/profileButton" />

<Button
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Friends"
    android:id="@+id/friendsButton"
    android:layout_marginTop="50dp"
    android:layout_below="@+id/datesButton"
    android:layout_alignRight="@+id/datesButton"
    android:layout_alignEnd="@+id/datesButton"
    android:layout_alignLeft="@+id/datesButton"
    android:layout_alignStart="@+id/datesButton" />

    <ToggleButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Available"
        android:id="@+id/toggleButton"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:checked="true"
        android:layout_toRightOf="@+id/friendsButton"
        android:layout_toEndOf="@+id/friendsButton" />


</RelativeLayout>
